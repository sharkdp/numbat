let reaction_time: Time = 0.5 second
let braking_power: Acceleration = 0.8 gravity

fn stopping_distance(v: Speed) -> Length = v × reaction_time + ½ v² / braking_power

assert_eq(stopping_distance(30 meter/second), 72.359 meter, 0.001 meter) # TODO: use km/h once we have prefixes
